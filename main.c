#include "shell.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - main function.
 * @argc : argument count.
 * @argv : argument vector.
 *
 * Return: 0 on success
 */
int main(int argc __attribute__((unused)), char **argv)
{
char *prompt = ( "$");
char *line;
int j; 
while(1)
{
printf("%s", prompt);
}
return (0);
}
